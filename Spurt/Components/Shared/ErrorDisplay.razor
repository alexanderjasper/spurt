@if (!string.IsNullOrWhiteSpace(ErrorMessage))
{
    <div class="error-container">
        <p class="error-message">@ErrorMessage</p>
        @if (ShowDismissButton)
        {
            <button @onclick="Dismiss" class="dismiss-button">×</button>
        }
    </div>
}

<style>
    .error-container {
        background-color: #ffebee;
        border: 1px solid #f44336;
        border-radius: 4px;
        padding: 12px;
        margin: 10px 0;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .error-message {
        color: #d32f2f;
        margin: 0;
    }

    .dismiss-button {
        background: none;
        border: none;
        color: #d32f2f;
        font-size: 20px;
        cursor: pointer;
        padding: 0 8px;
    }
</style> 